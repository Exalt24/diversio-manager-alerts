{
  "name": "diversio-manager-alerts",
  "version": "1.0.0",
  "description": "Manager alerts dashboard with hierarchical tree traversal",
  "scripts": {
    "setup:backend": "cd backend && pip install -r requirements.txt && python manage.py migrate && python manage.py load_seed_data",
    "setup:frontend": "cd frontend && npm install",
    "setup": "npm install && npm run setup:backend && npm run setup:frontend",
    "predev": "node -e \"const fs=require('fs');if(!fs.existsSync('frontend/node_modules')||!fs.existsSync('backend/venv')){console.error('\\n❌ Setup required! Run: npm run setup\\n');process.exit(1)}\" && npm install --silent",
    "dev:backend": "cd backend && python manage.py runserver",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,magenta\"",
    "test:backend": "cd backend && pytest -v",
    "test:frontend": "cd frontend && npm test",
    "test": "npm run test:backend && npm run test:frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Exalt24/diversio-manager-alerts"
  },
  "author": "Daniel Cruz <dcruz@up.edu.ph>",
  "license": "UNLICENSED"
}
